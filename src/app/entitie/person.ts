import { PersonType } from "./person-type"

export class Person{
    document!: string;
    documentType!: DocumentType;
    email!: string;
    enabled!: boolean;
    name!: string;
    person!: PersonType;
    personId!: string;
}